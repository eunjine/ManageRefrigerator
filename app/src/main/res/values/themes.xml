<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ManageRefrigerator" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/dark_green</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="BottomNavigationTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/dark_green</item>
        <item name="android:textColorSecondary">@color/dark_green</item>
    </style>

    <style name="Theme.ManageRefrigerator" parent="Base.Theme.ManageRefrigerator" />
</resources>